import 'package:flutter/material.dart';

class ScreenSize {
  static width(BuildContext context) => MediaQuery.of(context).size.width;
  static heigh(BuildContext context) => MediaQuery.of(context).size.height;
}
